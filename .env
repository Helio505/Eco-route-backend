# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema
# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings
# DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"

# Url para o prisma se conectar quando API estiver rodando dentro do docker
DATABASE_URL="postgresql://postgres:postgres@database:5432/ecoroute?schema=public"
# Url para o prisma se conectar quando API estiver rodando fora do docker
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ecoroute?schema=public"
PORT=3000
CORS_ORIGIN=http://localhost:3000